// operators: they are used to perform operations on operands

// 1)arithmetic operators: to perform mathematical operations
        // addition ==> +
        let a=60
        let b=30
        console.log(a+b);
        
        // subtraction:
        console.log(a-b);

        // multiplication
        console.log(a*b);

        // division
        console.log(a/b);
        
        // modulus
        console.log(a%b);
        console.log(3%1);
        
        // floor division
        // console.log(20//2);  error


        a++
        console.log(a);
        ++a
        console.log(a);
        console.log(a++);
        console.log(a);
        console.log(++a);
        


        console.log(b);
        b--
        console.log(b);
        --b
        console.log(b);
        console.log(b--);
        console.log(b);
        console.log(--b);
        
        
// 2)assignment operators:
        // to assign a value to a variable
        // equals to =
        let c = 55

        // 1)addition assignment operator(+=)
              c+=5
              console.log(c,'addition');
              

        // 2) subtraction assignment operator
              c-=10
              console.log(c,'subtraction');
        
        // 3) multiplication assignment operator
              c*=2
              console.log(c,'multiplication');
              
        // 4)division assignment operator
              c/=2
              console.log(c,'division');
        
        // 5)modulus assignment operator
              c%=2
              console.log(c,'modulus');

// 3) Comparision operators
                // it compares the two values and returns the boolean values 


                // 1) == double equals to 
                        let k=30
                        let k1='30'
                        console.log(k==k1); 
                        // true output
                // 2) != not equals to 
                        console.log(c!=k1);

                // 3) === strictly equal to
                        console.log(k===k1);

                // 4) !== strictly not equal to 
                        console.log(k!==k1);

                // 5) > greater than
                        console.log(c>k1);

                // 6) < less than
                        console.log(c<k1);

                // 7) >= greater than equal to 
                        console.log(c>=k1);

                // 8) < less than equal to 
                        console.log(c<=k1);

                // 9) && ampersand

                
